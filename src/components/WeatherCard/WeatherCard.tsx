import * as React from 'react';

import * as css from './styles/WeatherCard.scss';

interface IWeatherCardProps {

}

export const WeatherCard: React.FunctionComponent<IWeatherCardProps> = props => {

    return (
        <div className={css.container}>

        </div>
    );
};